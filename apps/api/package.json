{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node src/server.mjs",
    "start": "node src/server.mjs",
    "db:migrate": "node -e \"console.log('Apply SQL from apps/api/db/migrations/*.sql to DATABASE_URL')\"",
    "build": "node --check src/server.mjs",
    "lint": "node --check src/server.mjs"
  }
}
